import { Product } from "./product";

export class Cart {
    // constructor(p: Product, qty: number) {
    //     this.product = p;
    //     this.quantity = qty;
    // }
    product: Product;
    quantity: number;
}